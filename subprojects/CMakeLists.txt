if (BUILD_SPADAPTER)
    find_program(PYTHON_PIP pip3)
    if (PYTHON_PIP)
        add_subdirectory(sourcepawn)
    else()
        message(FATAL_ERROR "Python pip not found")
    endif()
endif()

if (BUILD_EXAMPLE)
    add_subdirectory(example)
endif()
